#include <stdio.h>
#include <string.h>

void increment(char x){
	char x;
	printf("--analysing process--\n");
	for (int i=0;i<=x;i++){
		printf("The word is %d and address is %p\n",x[i],&x[i]);
	}

}
int main(){
	char arr[100];
	printf("enter the words\n");
	fgets(arr,100,stdin);
	for(int 1=0;i<=arr[i];i++){
		char x=arr[i];
	}
}